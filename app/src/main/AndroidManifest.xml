<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SmartFarm">

        <activity
            android:name=".menu.MenuSelectionActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".hidroponik.HidroponikActivity"
            android:exported="false" />
        <activity
            android:name=".mediatanah.MediaTanahActivity"
            android:exported="false" />

        <activity
            android:name=".base.BaseSmartFarmActivity"
            android:exported="false" />

        <!--
            ScheduleAlarmReceiver dan BootReceiver sudah tidak digunakan.
            Logika penjadwalan sekarang dijalankan oleh mikrokontroler (MCU).
            Receiver di bawah ini dipertahankan sebagai no-op agar tidak crash
            jika masih ada alarm lama yang tertrigger. Bisa dihapus sepenuhnya.
        -->
        <receiver
            android:name=".mediatanah.ScheduleAlarmReceiver"
            android:exported="false"
            android:enabled="false" />

        <receiver
            android:name=".mediatanah.BootReceiver"
            android:exported="false"
            android:enabled="false" />

    </application>

</manifest>